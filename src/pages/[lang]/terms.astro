---
import TermsPage from '../terms.astro';
import { LEGAL_LOCALES } from '../../i18n/utils';

export function getStaticPaths() {
  return LEGAL_LOCALES.map((lang) => ({
    params: { lang },
  }));
}
---

<TermsPage />
